# Week M02 Oral Exam — 同一个 64M MiniMind 在 8×V100 上的四种放置方式

> **闭卷。提交答案或自评前不要打开 `05_REFERENCE_ANSWERS.md`。**
> 每题括号内依次是：限时（分钟）· 期望 AI 等级 · 考察维度。
> AI 等级：`A0` = 全程不查文档不问 AI；`A1` = 可查本周 `01_FOUNDATIONS.md` 与 `lab/` 源码，但必须在答案里标注查了什么。
> 维度：`K` 知识回忆 · `A` 应用推算 · `D` 故障诊断 · `E` 工程取舍。
>
> 全卷 27 题，建议分两次完成（第 1 次 R+E+A，第 2 次 D+P+T），两次日期不同即可作为 Gate 的"两个不同日期的证据"。
> 代码基线：MiniMind commit `7a6fddd`，PyTorch 2.1.0，8×V100 32 GB。所有默认值以该基线为准。
>
> **公司边界（作答时同样适用）**：本周全部多卡实验在公司 8×V100 上进行；日志、trace、checkpoint、图片、`nvidia-smi topo -m` 原文和任何绝对性能数字都留在公司内。凡是题目要求你"报告"或"记录"的量，一律只写**抽象比值**（例如 $t_{FSDP}/t_{DDP}$）、**是否连续**、**次数**、**`PASS/FAIL`/三档区间**，不写绝对 ms、不写绝对 GB、不写拓扑矩阵。
> **数字来源约定**：本周截至出卷时**没有任何实测数据**。所有需要给数字的题，答案必须是由配置推出的 `计算` 值或标注清楚的 `估算` 值；把任何数字说成"我实测到"即判 0 分。

---

## Recall

### M02-R-01（4 min · A0 · K）

关于本周在 V100 上的数值配置：

1. `torch.cuda.is_bf16_supported()` 判断的是什么硬件条件？V100 为什么不满足？在 MiniMind 的训练脚本里，把 `--dtype bfloat16` 原样跑在 V100 上，**报错发生在哪一步**（哪个调用进入时抛出）、异常类型与信息是什么？
2. MiniMind 五个训练脚本的 `--dtype` 默认值是什么？改成 `float16` 之后，`GradScaler` 的 `enabled` 参数如何取值？五个脚本里哪一个没有 `GradScaler`？
3. 在 `autocast(dtype=float16)` 下，写出至少 4 个会被降到 fp16 执行的算子、至少 4 个会被保留在 fp32 的算子；并说明 `param` 与 `param.grad` 各自是什么 dtype。

### M02-R-02（3 min · A0 · K）

关于 SDPA 后端：

1. torch 2.1 的 flash 后端覆盖哪个 compute capability 区间？V100 的 compute capability 是多少？结论是 V100 上 `F.scaled_dot_product_attention` 会落到哪些后端？
2. 写出手动指定后端的那个上下文管理器的名字和它的三个开关参数名。
3. 在 `B=8, T=512, H=8` 的本周 worked example 下，走 math 后端时会物化出一个什么 shape 的张量？fp16 下它每层占多少字节（给算式与数字）？

### M02-R-03（4 min · A0 · K）

不查文档写出 MiniMind 默认配置下的以下值：

1. `hidden_size` · `num_hidden_layers` · `vocab_size` · `num_attention_heads` / `num_key_value_heads` · `head_dim` · `intermediate_size`（给算式与结果）· `num_experts` · `num_experts_per_tok` · `moe_intermediate_size` · `router_aux_loss_coef` · `tie_word_embeddings`。
2. 写出 **dense 每层参数量**与 **MoE 每层参数量**（各给到个位数），并说明两者的差额来自哪两项。
3. 写出 dense 模型总参数量与 MoE 变体总参数量（各给到个位数），以及 MoE 变体的**每 token 激活参数量**。

### M02-R-04（4 min · A0 · K）

关于 PyTorch 2.1 的 FSDP：

1. 写出 `ShardingStrategy` 的三个本周会用到的枚举值。
2. `FULL_SHARD` 与 `SHARD_GRAD_OP` 在**一个 step 内的集合通信构成**上差在哪一类通信、差多少次？以 wrap 粒度 = 每个 `MiniMindBlock`（8 层）+ embedding 单独 wrap 计，分别写出两者每步的 AllGather 次数、ReduceScatter 次数与总次数。
3. 写出 `StateDictType` 的三个取值，以及 2.1 里 `backward_prefetch` 与 `forward_prefetch` 的默认取值。

### M02-R-05（4 min · A0 · K）

1. 写出 `dist.all_to_all_single` 的完整参数列表（按位置顺序），并用一句话分别解释 `input_split_sizes` 与 `output_split_sizes` 的含义——说清楚每个列表的**长度**是什么、第 $j$ 个元素**计的是哪个方向的多少个什么**。
2. `dist.new_group(ranks)` 对"不在该组里的进程"有什么要求？不满足会怎样？
3. torch 2.1 里 NCCL process group 的默认 `timeout` 是多少？哪个环境变量能把"超时后静默等待"改成"超时后崩溃"？
4. MiniMind 的 `_resume.pth` 里存了哪些键？写出恢复时 `step` 的换算式（用 `saved_ws` 与 `current_ws` 表示）。

---

## Explain

### M02-E-01（8 min · A0 · K）

关于 MiniMind 的 MoE aux loss：

1. 写出代码里那一行的形式 `(load * scores.mean(0)).sum() * E * coef`，并把其中每个因子对应到数学式的哪个符号（$f_e$、$P_e$、$E$、$\lambda$）；说明 `scores` 是在什么精度下算的、为什么。
2. 去掉 $\lambda$ 之后的那个量（即 $E\sum_e f_eP_e$），在**完全均衡**与**完全坍缩到单个专家**两种极端下各等于多少？给推导。
3. 这个 aux loss 的梯度经由哪个因子流回 router？另一个因子为什么不贡献梯度？
4. 列出 MiniMind 当前 MoE 实现**没有**的四样东西（相对于工业级 MoE 实现），并说明其中哪一样直接导致了本周"木桶效应"实验能被观察到。

### M02-E-02（8 min · A0 · K）

关于 DDP 的梯度通信：

1. 写出 DDP 在 $W$ 卡 $A$ 次累积下实际计算的梯度估计量 $\hat g$ 的表达式，并指出它等于全局 token 平均梯度 $\nabla\mathcal{L}$ 的充要条件。
2. DDP 把梯度按什么单位打包通信？默认包大小是多少？在本周的 64M dense 模型上大约打成几个包（给算式）？
3. 这些包在反向过程中是按**就绪顺序**还是**桶索引顺序**启动的？这个选择带来的一个好处和一个坏处各是什么？
4. 在 EP=4/DP=2 的场景下，为什么不能用一个 `DistributedDataParallel` 原样包住整个 MoE 模型？说清哪一部分参数的通信组与其余部分不同，以及本周 lab 打算用什么替代做法。

### M02-E-03（10 min · A0 · K）

按顺序写出教学版 EP 在**一个 MoE 层的一次前向**里的六个步骤（从 router 输出到最终 combine 完成），每步写清：输入张量、输出张量、发生了哪种集合通信（如果有）。然后：

1. 写出不变量 I2（split 一致）的完整表述：对每一对 $(r,j)$，哪两个量必须相等？
2. 违反 I2 时，为什么表现是 hang 而不是立刻报错？请从"接收方在等待什么"这个角度解释。
3. 写出不变量 I3（往返等价）的表述，以及它在什么并行度下可以被一个 CPU 单测直接验证。
4. 统计：一个 MoE 层的一个训练 step（含反向）里，一共发生几次 `all_to_all_single`？分别是哪几次？

### M02-E-04（10 min · A1 · K）

关于 FSDP 的通信时机：

1. 对一个被 wrap 的子模块，分别说明它的 AllGather 在 **forward** 与 **backward** 中各发生在什么时刻、ReduceScatter 发生在什么时刻；`FULL_SHARD` 在 forward 结束后对该子模块的完整参数做了什么，`SHARD_GRAD_OP` 又做了什么。
2. wrap 粒度如何影响每步的集合通信**次数**与**峰值显存**？给出两个极端（wrap 到每一层 vs 只有一个根 FSDP）下这两个量的变化方向。
3. 2.1 的 `forward_prefetch` 默认值决定了 forward 里"通信与计算能否重叠"的什么性质？`BACKWARD_PRE` 在反向里补上了什么？
4. 写出 FSDP 每层 AllGather 的**负载** $S_\ell$ 与**每 rank 实际收发量**的关系式（用 $W$ 表示），并说明为什么这两个数不同。

### M02-E-05（10 min · A0 · K）

关于 fixed-global-batch 不变量 I1：

1. 完整写出 I1 的表述：在哪些条件同时成立时，两种 $(W, A, b)$ 配置的 loss 曲线应在 fp16 舍入误差内重合？请给出**五个条件**。
2. MiniMind 用 `setup_seed(42 + rank)` 让每个 rank 的种子不同。在当前默认配置下这为什么**不会**破坏模型初值一致？（说清是哪个机制保证的。）在什么配置改动下它会立刻破坏 I1？
3. 用 `DistributedSampler` 时，2 卡与 8 卡在第 $k$ 个 optimizer step 消费的样本集合是否相同？为什么？要让它们相同需要在 lab 里做什么改动？
4. 从数学上说明：为什么 SFT 数据（每条有效 token 数不同）下，"micro-batch 均值再平均"与"全局 token 均值"**必然**不等；这种不等会系统性地高估哪一类样本？

---

## Apply

### M02-A-01（15 min · A0 · A）— Gate 题

给定 8 张 V100（rank 0–7），MoE 配置 `num_experts=4`、`num_experts_per_tok=1`，采用 EP=4 / DP=2，EP 组由连续 rank 组成。

1. 画出一张 8 行的表，逐 rank 写出：所属 EP 组的成员集合、该 rank 持有的 expert id、该 rank 的**专家参数梯度**要和哪些 rank 做 all-reduce、该 rank 的**非专家参数**（attention / embedding / norm）梯度要和哪些 rank 做 all-reduce。
2. 用同样的格式再补两行配置：EP=2/DP=4 与 EP=1/DP=8，各写出 EP 组划分、每 rank 持有几个 expert、专家梯度的 DP 组。
3. 若要做 EP=8/DP=1，必须先改哪个配置项、改成多少？写出约束条件（用 $E$ 与 $E_p$ 表示）。
4. 画出 EP=4/DP=2 下**一个 MoE 层一个训练 step**的完整通信序列（前向 + 反向 + 梯度同步），按时间顺序列出每一次集合通信的：类型、参与的 process group、传的是什么。

### M02-A-02（12 min · A0 · A）

worked example：$B=8$、$T=512$、`hidden_size=768`、fp16、top-1、EP=4/DP=2、专家均分。只算**一个 MoE 层**。

1. 每 rank 每层有多少个 token？`x_flat` 的 shape 与字节数是多少（给算式）？
2. router 的 `scores` 张量 shape、dtype 与字节数？counts 向量 `c_r` 的 shape、dtype 与字节数？
3. dispatch 时该 rank **发送**的字节数上限是多少？为什么是"上限"而不是精确值？
4. dispatch 时该 rank **接收**的字节数：给出（a）完全均衡时的期望值，（b）全部 token 都路由到本 rank 这个专家时的上限值。两者都给算式。
5. 接收到 $n_{recv}$ 个 token 后，本地 expert 的 `gate` 输出张量 shape 是什么？在第 4 问的上限情形下它占多少字节？这样的张量在一次 FFN 前向里有几个？
6. 若改成 top-2（其余不变），第 3 问的发送量、第 4 问的期望接收量、第 4 问的上限接收量分别变成多少？说明为什么其中有一个**不变**。

### M02-A-03（12 min · A0 · A）

在 8 卡上比较 DDP 与 FSDP 的每步通信量（64M dense 模型，wrap 粒度 = 每个 `MiniMindBlock` + embedding 单独 wrap；`MixedPrecision(param_dtype=fp16, reduce_dtype=fp16)`）。

1. DDP：写出每步 all-reduce 的**总负载**字节（注意梯度是什么 dtype，给算式）；按默认桶大小算出桶数；用 ring all-reduce 算出每 rank 收发各多少字节（给公式与数字）。
2. FSDP：写出单个 `MiniMindBlock` 的 AllGather 负载 $S_\ell$ 与 embedding 的 AllGather 负载（给算式）。
3. FSDP `FULL_SHARD`：写出每步的总负载与每 rank 实际收发量（给算式与数字）。
4. FSDP `SHARD_GRAD_OP`：同样写出每步集合通信次数、总负载与每 rank 实际收发量。
5. 把三者的"每 rank 实际字节"与"每步集合通信次数"排成一张对照表。据此，如果只看字节量，应该预测谁最快？这与本周的工作假设一致吗？如果不一致，说明差异必须由哪一类项解释。

### M02-A-04（12 min · A1 · A）

在第 A-02 题的同一配置（每 rank 4096 token，$E=4$，$k=1$，EP=4）下引入 capacity。设某一层某个 rank 的 router 给出的 token→expert 计数向量为 $c=[2867, 410, 410, 409]$。

1. 写出 capacity 的定义式 $C=\text{cf}\cdot\frac{Nk}{E}$ 里每个符号在本例中的取值，并算出 cf = 1.0 与 cf = 1.5 时的 $C$。
2. cf = 1.0：这一 rank 被丢掉多少个 token？占本 rank token 的百分之几？同时有多少个 capacity 槽位是空的（padding 浪费）？把这两个数写在一起，说明它们的关系并解释为什么。
3. cf = 1.5：重算第 2 问的三个数。
4. dropless（MiniMind 现状）：EP 组内 4 个 rank 的计数向量都等于 $c$。rank 0 上的 expert 0 会收到多少 token？折合多少字节？与 A-02 第 4 问的"均衡期望"和"上限"相比处于什么位置？
5. 算出本例的热点比 $\max_e f_e\cdot E$。若 dropless 下 step time 正比于最慢 rank 的 expert token 数，cf = 1.5 + drop 相对 dropless 在**计算量上界**上改善了多少倍？代价用一个数字表示是什么？

---

## Debug

### M02-D-01（10 min · A0 · D）

症状：把 EP 从 1 改到 4 后，8 卡训练在第一个 MoE 层就停住。没有任何异常抛出，也没有日志继续输出；`nvidia-smi` 显示 8 张卡利用率都是 100%，显存不再增长；进程一直存在。等到约 30 分钟后才有 rank 陆续退出。

1. 这个症状最可能破坏了本周哪一条不变量？写出编号与完整表述。
2. 为什么"等 30 分钟才退出"这一现象本身就是一条重要证据？它指向哪个默认配置项？
3. 给出你的前三个假设（按可能性排序），每个假设配一条**能在 5 分钟内做完**的区分实验。
4. 写出两条应该被加进 `ep_moe.py` 的断言（用 `input_split_sizes` / `output_split_sizes` / `input.shape[0]` / `output.shape[0]` 表述），说明它们能不能覆盖第 1 问的不变量的**全部**内容；不能的话，缺的那部分要靠什么手段查。
5. 你打算怎么在**不占用 8 卡配额**的前提下先复现这个 bug？写出环境、后端、进程数与要断言的东西。

### M02-D-02（12 min · A1 · D）

症状：8 卡 FSDP 训练到第 1000 个 optimizer step 时手动 kill 掉一个 rank，随后用 4 卡从最近的分片 checkpoint 恢复。两种失败先后出现：

- 第一次尝试：加载模型报 `size mismatch`，另有一条关于 `flat_param` 长度不匹配的报错。
- 第二次尝试（换了保存方式后模型能加载）：训练能跑，但打印出来的 `step` 从 1000 继续，学习率调度看起来"退回去了"。

1. 第一个失败破坏的不变量是什么？用一句话说清"FSDP 分片是哪几个量的函数"。
2. 分别说明 `FULL_STATE_DICT(rank0_only)`、`LOCAL_STATE_DICT`、`SHARDED_STATE_DICT` 三种保存方式在 8→4 卡恢复时各自要做什么、各自的代价是什么；本周 lab 的默认选择应该是哪一个，理由是什么。
3. 优化器状态怎么办？写出 2.1 里对应的两个 FSDP API 名字，并说明如果跳过它们、直接 `optimizer.load_state_dict(...)` 会发生什么。
4. 第二个失败：写出 MiniMind 里 `step` 的换算式，代入本例算出 4 卡恢复时 `step` 应该是多少。这个换算在什么前提下成立？本例里如果同时把 `batch_size` 从 8 改成 16，换算还对吗？不对的话应该按什么重算？
5. 写出你验证"恢复成功"的两条可提交判据（都要满足公司边界，即只带出抽象量）。

### M02-D-03（12 min · A0 · D）— Gate 跨场景 debug

症状：EP=4 的 MoE 训练跑到第 300 步左右开始变慢，到第 800 步时单步时间约为前 100 步的 3 倍（比值 `估算`，来自公司内计时器）。逐 rank 计时显示：EP 组内有 1 个 rank 的 `t_compute` 明显高，其余 3 个 rank 的 `t_wait` 明显高；4 个 rank 的 `t_compute + t_wait` 大体相等。`logits_loss` 仍在下降。

1. 写出这个症状对应的木桶效应公式：EP 组内的 step time 等于什么的什么？
2. 给出四步定位路径（从计时分布开始，到 router 内部量结束），每一步写清"看哪个量、期望值是多少、异常长什么样"。
3. 写出你会用 `router_stats.py` 打印的三个量，以及每个量的"健康"取值。
4. 提出两个互斥假设来解释"为什么是第 300 步之后才出现"，并各给一个能区分它们的实验。
5. 如果加大 aux 系数把热点压下去了，你应该拿哪一个量当主指标来判断"这次修复是不是白赚的"？为什么不能拿 `aux_loss` 本身当主指标？
6. 本题的所有结论要提交给周报，按公司边界你能写出哪些字段、不能写哪些？各举两例。

### M02-D-04（10 min · A1 · D）

症状：把 8 卡 DDP 换成 8 卡 FSDP `FULL_SHARD` 之后，峰值显存比 DDP **更高**（比值 $m_{FSDP}/m_{DDP} > 1$，`估算`）；单步时间也更长。

1. 显存更高这一条，与本周对 FSDP 的基本预期矛盾。写出"正常情况下 FSDP 应该省下哪三类量、每类省多少倍"。
2. 给出三个能解释"显存反而更高"的具体原因，每个都要落到一个具体的构造参数或 wrap 结构上。
3. 其中有一个原因会让 FSDP 的前向峰值直接等于"整个模型的完整参数"。是哪一个？怎么在不跑满 8 卡的情况下先验证它？
4. 单步时间更长这一条，是不是和显存问题同一个根因？给出"是同一个根因"和"是两个独立问题"两种情况下，各自会看到什么不同的证据。
5. 写出修复后你要重新记录的四个抽象量（含它们的取值形式），确认都符合公司边界。

### M02-D-05（12 min · A0 · D）

同一份 8 卡 fp16 训练日志里同时出现两个异常，请分开处理。

**异常 A**：`scaler.get_scale()` 的打印序列为 65536 → 32768 → 16384 → … 一路减半，中途从未回升；约 20 步后 `loss` 打印出 `nan`。`lr` 一直在正常推进。

**异常 B**：同一个代码在 2 卡×accumulation 4 与 8 卡×accumulation 1（两者 global batch 相同）下跑，**第 1 个 optimizer step** 的 loss 就相差约 $10^{-2}$。

1. 异常 A：先判断这是"fp16 动态范围不够"还是"模型本身发散"。写出你的判据（至少两个可打印的量），并说明"偶尔跳步"与"反复跳步"在日志形态上的区别。
2. 异常 A：如果判定为发散，为什么"换个数值格式"不能修？在 V100 上你**能**换的替代格式有哪些、不能换的是哪个、为什么？
3. 异常 A：`loss.item()` 在跳步时照常打印出正常值，参数却没更新。解释这个现象的机制，并给出一个能直接证明"参数确实没动"的检查。
4. 异常 B：这破坏了哪条不变量？$10^{-2}$ 这个量级本身说明了什么——它是不是可以用 fp16 累加顺序解释？给出你判断的依据（合理差异的量级是多少）。
5. 异常 B：列出四个"不合理来源"的候选，并为每一个给一条第 1 步就能打印出来的证据。
6. 异常 A 与异常 B 有没有可能是同一个根因？给出你的判断和一条能证伪的观测。

---

## Design

### M02-P-01（14 min · A1 · E）

设计一次 aux 系数 $\lambda$ 的消融，目标是把"$\lambda$ 太小导致热点"与"$\lambda$ 太大导致专家分工丧失"两种失败**分开测量**。扫描点为 $\lambda\in\{0, 10^{-3}, 10^{-2}, 10^{-1}\}$（MiniMind 默认值不在其中，请说明默认值是多少、你要不要补进扫描）。

1. 写出你的假设：$\lambda$ 从 0 增大时，哪两个量分别单调往哪个方向动，它们在什么区间会出现"交叉"。
2. 设计主指标与副指标各一个（都要能在公司边界内以抽象形式带出），并说明为什么主指标不能选 `aux_loss`。
3. 给一个能直接检测"router logits 被拉平"的量（不是 loss），写出它在完全均衡与完全坍缩时的取值。
4. 这个实验的**对照基线**应该是什么？为什么必须有它？
5. 写出失败边界：在什么观测下你会宣布这次消融 `INCONCLUSIVE` 而不是给结论。
6. 给出可验证的下一步：如果扫描显示 $10^{-2}$ 最优，你下一步要做的一个实验是什么，用来排除什么替代解释。

### M02-P-02（14 min · A1 · E）

公司这台 8×V100 的卡间不是全 NVLink（NV1/NV2 混合，可能有跨 PCIe 路径，`用户自述待核验`）。EP=4 时有两种分组方式：

- 方案 A：`{0,1,2,3}` / `{4,5,6,7}`
- 方案 B：`{0,2,4,6}` / `{1,3,5,7}`

1. 用第 5 节的符号通信模型说明：这两种分组会通过哪一个符号影响 `all_to_all` 的时间？该符号由组内的哪条链路决定？
2. 设计一个能在 10 分钟内做完、且不带出拓扑细节的对照实验：跑什么、控制哪些变量、记录哪个比值、判据阈值定在多少。
3. 如果两种分组的比值落在 1.0 附近（比如 1.05），你应该得出"拓扑不重要"还是别的结论？给出至少两种替代解释。
4. 这个实验对**非专家参数**的 all-reduce 有没有影响？为什么？
5. 若结论是方案 A 明显更好，写出你会把它固化到 lab 哪个位置、以什么形式（不能写死拓扑信息）；再写出这个结论的失败边界（换机器 / 换 EP 度 / 换 top-k 时它还成立吗）。
6. 把结论写进周报时，哪些字段可以带出？给出你打算写的那一行的确切形式。

### M02-P-03（15 min · A1 · E）

GRPO 三个模型：Policy 63.9M（要训练）、Reference 63.9M（冻结）、Reward 1.8B（冻结，约为 policy 的 30 倍）。当前 MiniMind 的做法是每张卡都加载全部三个模型。请为 8×V100 设计角色放置。

1. 先给显存账：分别写出 Policy 的 fp32 训练态（参数 + 梯度 + Adam）、Reference 的 fp16 权重、Reward 的 fp16 权重各占多少（给算式）。再说明"Policy 用 8 卡 FSDP 分片"这一步在每卡上到底省下了多少、没省下什么。
2. 给出你的放置方案（哪些 rank 做什么），并说明为什么把 Reward 单独拿出来是这里收益最大的一步。
3. 写出同步版一轮的完整时序：从 rollout 卡开始，到下一轮 rollout 卡拿到新权重为止，逐步写出计算/通信动作与使用的 process group；权重同步那一步传多少字节（给算式）。
4. 判断瓶颈在哪一步，并给出你的理由（要从"每次前向处理多少 token / 由什么主导"这个角度论证，不是只说"生成慢"）。
5. 在你的方案里，8 张卡在一轮中的空闲比例大概是什么形态？给一个能测量它的抽象量（符合公司边界）。
6. 写出这个设计的失败边界与可验证下一步：什么观测会让你退回"全复制 + Policy-FSDP"？

### M02-P-04（14 min · A1 · E）

设计 EP=1/2/4/8 的扩展实验（Day 3），目标是验证/证伪"EP 越大越快"。

1. 写出实验矩阵：四个配置各自的 EP 组划分、每 rank 专家数、`num_experts` 是否需要改。列出必须固定住的变量（至少四个）。
2. 写出你的假设与它的机制解释：随 EP 增大，哪一项开销**下降**、哪两项开销**上升**？
3. 每个配置要记录哪些量？全部写成公司边界允许的抽象形式（比值、次数、三档区间）。
4. 这个实验必须先过哪一个 CPU 单测才允许上 8 卡？该单测验证的是什么不变量？
5. 若结果是 EP=2 最快、EP=8 最慢，列出两个能解释它的机制，并各给一个区分实验。
6. 写出这个实验的失败边界：在什么情况下 EP 扫描的结论对更大的模型**不可外推**？

---

## Trade-off

### M02-T-01（14 min · A1 · E）

capacity factor 是在三件事之间做取舍：丢 token 造成的质量损失、padding 造成的计算与显存浪费、负载不均造成的设备空等。

1. 用 A-04 的数字支撑一个论证：为什么"质量（不丢 token）、计算与显存有上界、无空等"这三件事最多同时满足两件？分别写出三种典型取法各放弃了哪一件。
2. cf 从 0.75 提到 1.5，分别说明它对：丢 token 比例、padding 比例、峰值显存、最慢 rank 计算量 这四个量的影响方向；哪一个是线性的？
3. `drop_tokens=False`（dropless）在数学上等价于什么样的 cf？它把成本转移到了哪里？
4. 给出你在**本周这台 8×V100 + 64M 模型**上的选择与理由，并明确说明它在什么条件下会变成错误选择（至少两个条件）。
5. 被丢掉的 token 在前向里输出什么？这对反向和 aux loss 分别有什么后果？
6. 如果换成一个专家数 64、跨 8 节点的大 MoE，你对第 4 问的答案会怎么变？说清是哪个量的量级变化推动了这个改变。

### M02-T-02（12 min · A1 · E）

Top-1 与 Top-2 路由的取舍。

1. 在 A-02 的配置下，写出从 top-1 换到 top-2 后，下面四个量各自变成多少倍：单层 dispatch 发送字节、单层期望接收字节、单层接收字节上限、本地 expert 的 FLOPs。有一个不是简单倍数关系，指出是哪个并解释。
2. `norm_topk_prob=True` 在 top-2 下起了什么作用？在 top-1 下呢？
3. top-2 对**负载均衡**是更容易还是更难？给出你的理由，并说明它对 aux loss 的取值范围有什么影响。
4. top-2 在什么条件下值得付这个代价？给出两个可观测的判据（不是"效果更好"这种笼统说法）。
5. 在本周这台机器上，若 top-2 让单步时间上升到某个比值以上你就放弃，这个阈值你会怎么定？说清你是用什么口径定的。
6. 写出一个能证伪"top-2 的收益来自更好的专家分工"这一说法的实验。

### M02-T-03（15 min · A1 · E）— Gate A1 题

请完整论证"64M MiniMind 上 FSDP 比 DDP 慢，但 27B 模型上 FSDP（或等价的分片方案）是必须的"。

1. 写出"64M 上更慢"的完整因果链（至少四段），每段之间的推导要显式。
2. 用 A-03 的数字说明这**不是**字节量问题：写出两个关键数字并做对比。
3. 那么它是什么问题？把你的解释写成一个含 $\alpha$（每次集合通信的固定延迟）与 $t^{cmp}_\ell$ 的不等式，并说明什么时候这个不等式会翻转。
4. 27B 上为什么 DDP 直接不可行？给出显存账（参数 + 梯度 + Adam 状态，fp32 口径），并与单卡 32 GB 对比。
5. 估计一个转折点：在保持层数与 8 卡不变的前提下，`hidden_size` 大约要到多少量级，$t^{cmp}_\ell$ 才能盖住 AllGather 的固定延迟？写清你的估算口径与所有假设——这是 `估算`，不是实测。
6. 写出这个论证的失败边界：什么观测会推翻你的因果链？以及一个可验证的下一步（要能在本周的 8 卡预算内做完）。

### M02-T-04（14 min · A1 · E）

GRPO 的两种放置方式取舍：**方案甲**"每卡全复制三个模型 + Policy 用 8 卡 FSDP 分片"，**方案乙**"角色拆分：rank 0–3 policy、4–5 rollout、6–7 reward，同步版"。

1. 分别写出两种方案下每卡的静态显存构成（用 P-03 第 1 问的数字）。哪一方案的静态显存更低？低多少？
2. 分别写出两种方案下一轮的通信构成。方案乙多出了哪几类通信、少了哪几类？
3. 方案乙的同步版有一个结构性缺陷：某些卡在一轮的大部分时间里没有工作。指出是哪些卡在哪个阶段空闲，并说明为什么"同步"这个约束是空闲的直接来源。
4. 在什么条件下方案甲反而更好？给出至少两个条件，每个都要能被观测判定。
5. 方案乙里 rollout 卡使用的是哪一轮的权重？如果去掉"每轮 broadcast 完成后才生成"这个约束会得到什么？这会引入 RL 里的哪一类偏差？
6. 本周 Gate 不依赖这一天的产出。给出你在"时间不够"时的取舍方案：保留哪一部分作为证据、放弃哪一部分、放弃后周报里对应结论应该标成什么状态。

---

## 提交方式

- 每题写清：结论 + 推导/证据 + 你查过什么（A1 题必填）。
- 所有涉及数字的题，标注该数字是 `计算`（由配置推出）还是 `估算`（有假设的量级判断）。
- 所有涉及公司机器的题，只写抽象比值、是否连续、次数、`PASS/FAIL` 或三档区间。
- 两次作答的日期分别记在卷首，作为 Gate 的"两个不同日期的证据"。

# Week M03 Oral Exam — 一个 64M MiniMind 在 8×V100 上的三本账

> **闭卷。每题限时见括号。提交答案或自评前不要打开 `05_REFERENCE_ANSWERS.md`。**
> 括号内依次是：限时（分钟）· 期望 AI 等级 · 考察维度。
> AI 等级：`A0` = 全程不查文档、不问 AI、不看本周任何文件；`A1` = 只可查官方文档与本周 `01_FOUNDATIONS.md`、`lab/` 源码，且必须在答案里写明查了哪一处。
> 维度：`K` 知识回忆 · `A` 应用推算 · `D` 故障诊断 · `E` 工程解释与取舍。
>
> 全卷 **27 题**，建议分三次完成：第 1 次 `Recall + Explain`（61 min），第 2 次 `Apply + Debug`（136 min），第 3 次 `Design + Trade-off`（102 min）。三次日期不同即可满足 Gate 的"两个不同日期的证据"。
>
> **标了「Gate 题」的四道是 Gate 判据本身**：`M03-A-01`、`M03-D-01`、`M03-D-02`、`M03-T-04`。这四道必须单独计时、单独判分。
>
> 代码基线：MiniMind commit `7a6fddd63a30c06b2fdd5fac4089922b29bc841b`，PyTorch **2.1.0**，8×V100 32 GB（sm70）。所有"默认值"以该基线为准。
>
> **模型来源边界**：本周 `lab/src/mm_v100/model.py` 是**周包自带的、与 MiniMind 形状对齐的实现**，不是 MiniMind 本体，不含 MiniMind 的权重与 tokenizer。因此本卷所有字节账、通信账、数值账的结论都成立且可迁移，但**不要在答案里假设可以直接加载 MiniMind 发布的 checkpoint**。凡涉及"加载已有权重"的题，答案要写清权重从哪里来。
>
> **单位约定**：MB = 10⁶ B，GB = 10⁹ B，MiB = 2²⁰，GiB = 2³⁰。显存标称 32 GB 按 32 GiB = 34.36×10⁹ B 理解。写数字时标清单位。
>
> **公司边界（作答时同样适用）**：本周全部 GPU 实验在公司内网 8×V100 上进行。日志原文、profile、checkpoint、拓扑矩阵和任何绝对性能数字**留在公司内**。凡是题目要求你"记录"或"报告"的量，一律只写**比值**、**布尔**、**次数**、**第几步**、**`PASS` / `FAIL-MODEL` / `FAIL-SYSTEM` / `INCONCLUSIVE`**，不写绝对毫秒、不写绝对 GB、不写拓扑矩阵。
>
> **数字来源约定**：本周截至出卷时**没有任何 V100 实测数据**。凡要给数字的题，答案必须是由配置推出的 `计算` 值，或标注清楚的 `估算` 值。把任何未发生的运行说成"我实测到"，该题直接判 0 分。唯一的例外是两条**本机 CPU 实测**结论，它们出现在 `06_QUANT_LOWBIT.md` 里，引用时要标明是 CPU 实测。

---

## Recall

### M03-R-01（4 min · A0 · K）

不查任何文档，写出：

1. IEEE 754 binary16 的**最小正规数**、**最大值**、**1.0 处的 ULP（eps）**三个数，各给精确的 2 的幂次形式与十进制近似；再补上**最小 subnormal** 与**单位舍入 $u$** 两个数。
2. `torch.cuda.amp.GradScaler` 在 PyTorch 2.1 里的四个数值型默认参数的**名字与取值**。
3. MiniMind 的训练脚本里 `GradScaler` 的 `enabled` 参数是怎么取值的？这个取法在 V100 上意味着什么？

### M03-R-02（3 min · A0 · K）

写出 V100（sm70）在下列每一项上的能力判定（能 / 不能 / 未知），每项给一句话依据：

1. FP16 Tensor Core 矩阵乘；BF16；INT8 Tensor Core（IMMA）。
2. `F.scaled_dot_product_attention` 在 torch 2.1 下可能落到哪几个后端，不可能落到哪个，判据是什么 compute capability 区间。
3. `bitsandbytes` 的三类功能——`LLM.int8()`、8-bit 优化器、NF4/FP4——各自的 compute capability 门槛是多少，V100 上各是什么结论。
4. `torch.ao.quantization` 的 `convert_fx` 产物、`torchao`、`torch._int_mm` 三者在 V100 + torch 2.1 上各是什么结论。

### M03-R-03（5 min · A0 · K）

不查文档写出 MiniMind 默认 dense 配置：

1. 十二个值：`hidden_size`、`num_hidden_layers`、`vocab_size`、`num_attention_heads`、`num_key_value_heads`、`head_dim`、`intermediate_size`（给表达式与结果）、`tie_word_embeddings`、`dropout`、`rms_norm_eps`、`rope_theta`、`use_moe`。
2. 逐子模块写出**单层参数量**（把注意力四个投影、两个 head-dim 级 norm、两个 hidden 级 norm、FFN 三个矩阵分别列出），给到个位数。
3. 由此写出**全模型参数量**，给到个位数；并说明哪些张量**不进** `model.parameters()`。
4. `tie_word_embeddings=True` 带来的三个后果各是什么（一个参数量的、一个字节的、一个并行放置上的）。

### M03-R-04（4 min · A0 · K）

写出 PyTorch 2.1 里下列默认值，每个给出取值：

1. `DistributedDataParallel.__init__` 的 `bucket_cap_mb`（含单位）、`gradient_as_bucket_view`、`find_unused_parameters`、`broadcast_buffers`、`static_graph`。
2. `FullyShardedDataParallel.__init__` 的 `backward_prefetch`、`forward_prefetch`、`limit_all_gathers`、`use_orig_params`、`sync_module_states`。
3. `StateDictType` 的三个取值。
4. NCCL process group 的默认 `timeout` 是多少？哪个环境变量能把"超时后静默等待"变成"超时后崩溃"？

### M03-R-05（3 min · A0 · K）

关于内网 V100（torch 2.1.0 固定不动）的依赖边界：

1. 写出 `numpy`、`transformers`、`trl`、`bitsandbytes` 四个包**与 torch 2.1.0 相容的版本上限**，每个说明卡在哪里（是谁的哪条声明或哪个门槛）。
2. 写出四个**明确不能用**的包，每个给一句话原因；并说出这四个里有三个共享同一个结构性原因，那个原因是什么。
3. 内网装包失败最常见的单一根因是什么？对应的正确对策是哪个 pip 机制？为什么不该用 `--no-deps` 来解决它？
4. 离线加载模型时需要设的环境变量是哪一个？哪一个在 transformers 4.55 上写了也没用？

---

## Explain

### M03-E-01（8 min · A0 · E）

关于 $\ln 6400 = 8.764$ 这个数：

1. 为什么随机初始化的 MiniMind 第 0 步 loss 应当等于它？给推导，不要只给结论。
2. 把它换算成 bits 和 PPL，各是多少？换算用的那个常数是什么，为什么必须背下来？
3. 这个检查**能**验出哪一类接线错误、**不能**验出哪一类？给出至少一个它验不出来的具体故障，并说明为什么它对该故障免疫。
4. 为什么 loss 标量必须留在 fp32？用 8.764 附近的 fp16 分辨率与本周等价判定所需的分辨率，给一个定量论证。

### M03-E-02（8 min · A0 · E）

"loss 曲线平滑单调下降"这件事，到底证明了什么、不证明什么：

1. 用一句话写出"曲线好看"与"目标正确"之间的逻辑关系（哪个方向成立、哪个方向不成立），并说明曲线的**形状**和**渐近值**分别由什么决定。
2. 给出三个具体的 label 错位算例，说明每一个对 loss 曲线的可见影响是什么方向（更高 / 更低 / 几乎不动）。其中有一个会让 loss **看起来更好**，指出是哪一个并给一个带数字的算例。
3. 有一个算例连本周的两个数据契约不变量都查不出来，指出是哪一个，以及什么检查才抓得住它。
4. 为什么"把一条序列过拟合到 loss ≈ 0"完全无法验证位移是否正确？什么样的检查可以？给出那个检查的二值判据。

### M03-E-03（10 min · A1 · E）

关于"在 64M 这个规模上 FSDP 比 DDP 慢"这条经验：

1. 先把它当成一个待检验的假设。写出 `FULL_SHARD`（`reduce_dtype` 分别取 fp16 与 fp32 两种）相对 DDP 的**每 micro-batch 每 rank 线上字节比值**，给推导过程。这两个比值和 world_size 有关系吗？为什么？
2. 用第 1 问的字节数和 ring step 数，写出"FSDP 的纯线上时间更短"这个不等式，并把它化简成一个只含 $\alpha$（单次集合通信启动延迟）与 $\beta$（有效带宽）的临界条件。
3. 基于第 2 问，"慢在字节量"这个解释成立还是不成立？给出你的判断和它依赖的前提。
4. 如果第 3 问的答案是"不成立"，那么真正的来源可能是哪些？每一个来源给出**一个可以关掉或拧动的具体开关**。
5. 这些来源里有一个**只有换 batch size 才能分离出来**，指出是哪一类，并说明为什么只在一个 batch size 下测比值无法区分它。

### M03-E-04（8 min · A1 · E）

关于 `GradScaler`：

1. `init_scale` 的默认值为什么恰好是 $2^{16}$？把 fp16 不做缩放时的梯度窗口写出来，再写出乘上 $s$ 之后的窗口，指出上界落在哪个数上，并说明它和 MiniMind 的哪个默认超参是配套的。如果把那个超参改成 10，`init_scale` 应该往哪个方向调、调到什么量级？
2. 写出跳步的四个行为后果（提示按"日志上看得见 / 看不见"来组织），每个说明它为什么让这一档故障"静默"。
3. 为什么在 FSDP 路径上用普通的 `torch.cuda.amp.GradScaler` 本身就是一个静默 bug？应该用什么替代？给出机制层面的理由，不要只给 API 名字。
4. 给出一个"健康跳步率"的量化估计式（含启动阶段与稳态两项），并给出你会用什么阈值报警。

### M03-E-05（8 min · A1 · E）

关于 RL 里的两个数值恒等式：

1. 记 $r = p_{new}/p_{ref}$，$\log r = \log p_{new} - \log p_{ref}$。写出三个常见 KL 估计量 `k1` / `k2` / `k3` 的表达式，并说明各自的偏性、方差与符号性质。
2. 证明 `k3` 是 KL 的**无偏**估计，并证明它**恒非负**。两条证明各自用到什么前提？
3. 实现 `k3` 时用 `torch.expm1(log_r)` 而不是 `torch.exp(log_r) - 1`，理由是什么？如果实现里出现了负的 `k3`，该判定成精度问题还是实现问题？为什么？
4. DPO 在 policy 与 ref 逐位相同时，初始 loss 精确等于什么数？给推导，并说明这个数和 `beta` 有没有关系。若实测偏离这个数，按什么顺序查哪两件事？

---

## Apply

### M03-A-01（30 min · A0 · A）—— **Gate 题（A0 闭卷 30 分钟）**

给定一个**新的**配置（不是 MiniMind 默认值，`intermediate_size` 用 MiniMind 的同一个表达式算）：

```
hidden_size          = 1024
num_hidden_layers    = 12
num_attention_heads  = 16
num_key_value_heads  = 4
vocab_size           = 8192
tie_word_embeddings  = True
所有 nn.Linear 的 bias = False；每层含 q_norm / k_norm 两个 head-dim 级 RMSNorm
b (每卡 micro-batch) = 8
T                    = 512
world_size W         = 8
accumulation A       = 4
精度                 = fp16 AMP（autocast），优化器 AdamW
SDPA 后端            = mem-efficient
```

闭卷 30 分钟内，在纸上写出：

1. `head_dim` 与 `intermediate_size`（给表达式与结果）；单层参数量 $P_\ell$ 与全模型参数量 $P$，都给到个位数。
2. **DDP** 每卡的四项字节账：param / grad / 优化器状态 / 激活。另外把两笔容易漏记的项单独列出来并各给数字。给出每卡合计（含一个标 `估算` 的 CUDA context 项）。
3. **FSDP `FULL_SHARD`** 每卡的四项字节账，常驻项与瞬时项分开写。给出每卡合计。
4. 激活那一项要写出你用的**每 token 系数**及其来源；注意这个配置的 $d_{ff}/d$ 与 MiniMind 默认值不同，说明这对系数有没有影响、影响多少。
5. **一个 optimizer step 内的完整集合通信序列**，DDP 与 FSDP 各写一份，每一条给：类型、次数、每 rank 线上字节量、ring step 数、单 ring step 的 chunk 大小。DDP 那一份要分"当前 MiniMind 训练循环的写法"和"加上梯度累积不同步之后"两种。
6. 最后写一句：这个配置在 32 GB 卡上，DDP 与 FSDP 各能不能跑；如果只能跑一个，卡住的是四项里的哪一项。

### M03-A-02（10 min · A0 · A）

目标：全局 batch $G = 768$ 条序列，$W = 8$，$T = 512$，模型是 MiniMind 默认 64M，fp16 AMP，mem-efficient 后端。

1. 写出 $(b, A)$ 必须满足的约束，并列出至少五组满足它的整数解。
2. 每卡激活预算 5.0 GB（不含静态与 context）。用本周的每 token 激活系数反算 $b$ 的上限，给算式。
3. 在满足第 2 问的解里挑一组，写出你的选择和理由。理由必须同时覆盖：激活字节随谁变、每 optimizer step 的集合通信次数随谁变、以及每 step 的固定主机侧开销随谁变。
4. 如果训练循环里加上了梯度累积不同步的上下文，第 3 问的答案会不会变？为什么？

### M03-A-03（12 min · A0 · A）

一次 FSDP `FULL_SHARD` 训练在 $W = 8$、$b = 16$、$A = 4$、$T = 512$、`step = 1200` 处保存了 checkpoint。现在只剩 4 张卡，要"接着训"。

1. 写出 `step'` 的换算式与数值（说明你保持不变的是哪个量）。
2. 若要保持全局 batch $G$ 不变且 $b' = b$，`A'` 应取多少？给推导。写出 $G$ 与 $G'$ 两个数验证。
3. 如果**只改 $W$ 不改 $A$**，训练配方发生了什么变化？用两句话说清楚"Adam 看到的是什么"，并说明为什么 loss 曲线看不出来。
4. 学习率相位在 8→4 之后会不会偏？给推导，并写清这个结论依赖什么前提。
5. `StateDictType` 三个取值里，哪些能换 world_size 恢复、哪些不能？本周为什么要练那条**不是最省事**的路径？
6. 除了模型权重，还必须一起恢复的三样东西是什么？每样漏掉会怎样？
7. 恢复之后你用哪些判据判定"这是同一次训练的继续"？给出你会用的组合，并指出其中哪一条是充分的、哪些只是必要的。

### M03-A-04（10 min · A1 · A）

MiniMind 默认 64M（$d=768$，$L=8$，$H=8$，$V=6400$），fp16 AMP，$T = 1024$，每卡激活预算 24 GB。

1. 写出 mem-efficient 后端下的每 token 激活字节数，并算出 $b$ 的上限。
2. 写出 math 后端相对 mem-efficient 的**额外**每 token 每层字节数的表达式，代入数字；再算出 math 后端下 $b$ 的上限。
3. 两个上限的比值是多少？如果把 $T$ 从 512 换成 1024，这个比值会往哪个方向走？给出机制层面的解释。
4. 由此写出一句可用于日常诊断的规律：把 $T$ 翻倍之后，观察到显存增长**超过**多少倍时，应当怀疑走的是哪条分支？

---

## Debug

### M03-D-01（18 min · A1 · D）—— **Gate 题（A1 45 分钟中的前半）**

默写本周的三档静默故障判别表。要求：

1. 列出三档故障各自的名字与机理一句话。
2. 每一档给出**一个不变量**（写清它测的是什么对象、在训练循环的哪个位置算、用什么精度算）。
3. 每一档给出**一条最小检查命令或最小代码片段**（不超过四行），以及通过 / 不通过的判据。
4. 说明三档在 loss 曲线上分别长什么样、分别有没有报错。
5. 判定"DDP 梯度未同步"这一档时，有一种很自然的实现是**比较各 rank 的梯度范数**。说明为什么这只是必要条件而不是充分条件，正确的比较应该在什么层级做，依据的是集合通信的哪一条语义保证。再说明：在一个 64M 这样的小模型上，如果坚持用范数做判据，最可能观察到的失败形式是漏报还是误报，为什么。
6. 三档里有一个不变量必须用 fp64 累加，指出是哪一个并给出定量理由（用 fp32 会带来多大的相对噪声，为什么这个噪声会让该不变量变成一台假阴性机器）。
7. 三档之外还有第四种故障会伪装成其中一档，写出它是什么、靠哪一对特征识别。

### M03-D-02（20 min · A0 · D）—— **Gate 题（跨场景 debug 20 分钟）**

下面是一次运行的日志，只保留了数字列。配置：MiniMind 默认 64M，`--placement ddp`，$W = 8$，$b = 16$，$T = 512$，$A = 1$，fp16 AMP，pretrain 阶段，lr 余弦衰减自 5e-4，`grad_clip = 1.0`。表中 `loss` / `lr` / `scale` / `landed/att` / `n_lab` / `n_nonpad` 是 rank 0 的值，`I3` / `I5` 是跨 rank 量。

```
step   loss      lr          scale        landed/att   n_lab   n_nonpad   I3          I4          I5
   1   8.7684    5.000e-4    6.554e+04    0/1          7994    8010       0.00e+00    0.00e+00    3.12e-01
   5   8.7681    4.999e-4    4.096e+03    0/5          7902    7918       0.00e+00    0.00e+00    2.87e-01
  10   8.7679    4.997e-4    1.280e+02    0/10         8051    8067       0.00e+00    0.00e+00    3.41e-01
  20   8.7686    4.990e-4    1.250e-01    0/20         7977    7993       0.00e+00    0.00e+00    2.95e-01
  40   8.7683    4.960e-4    1.192e-07    0/40         8018    8034       0.00e+00    0.00e+00    3.08e-01
  80   8.7680    4.842e-4    1.084e-19    0/80         7960    7976       0.00e+00    0.00e+00    3.22e-01
```

列名含义：`I3` = 跨 rank 的梯度**逐元素**指纹的最大相对偏差（`lab/src/mm_v100/faults.py` 的 I3，注意它比较的是元素级而不是范数）；`I4` = 相对上一记录点的参数校验和位移，fp64 累加；`I5` = 跨 rank 的 loss 最大差。`landed/att` = 窗口内"已落地的 optimizer step 数 / 尝试数"。

限时 20 分钟，回答：

1. 这是哪一档故障？给出你的判定。
2. 你用的是**哪一对**特征？说明为什么这一对是**充分的**——即列举出还有哪些别的原因能造成其中单独一项，以及为什么这一对把它们全排除掉了。
3. 指出表中有**两列各自单独看只是必要条件**，分别给出一个反例：一个正常 run 也会出现该列的这个表现，或者另一个完全不同的故障也会出现该列的这个表现。
4. `scale` 这一列还额外告诉了你一件事——它把这一档故障的**两个子情形**区分开了。写出这两个子情形，说明这份日志属于哪一个，判据是什么。
5. 如果这是**另外两档**中的某一档，这张表会有哪一列不一样？各写一行。
6. 写出你要做的**下一个单一测量**（一行代码即可），以及它的两种结果各自把你导向什么结论、什么修复动作。
7. `lr` 这一列在本档故障下的行为，对训练造成了什么额外后果？

### M03-D-03（12 min · A1 · D）

一次 FSDP `FULL_SHARD` 运行（MiniMind 默认 64M，$W=8$，按 `MiniMindBlock` 做 auto-wrap）打出了这些抽象量：

```
collectives_total (per micro-batch) = 3
mem_ratio  (FSDP peak / DDP peak)   = 0.93
step_time_ratio (FSDP / DDP)        = 2.4
无报错，loss 曲线平滑下降
```

1. 手算这个配置下 `collectives_total` **应该**是多少，给推导（要说清 unit 数怎么来的）。
2. 观测值 3 说明发生了什么？写出从根因到这三个观测量的完整症状链。
3. 写出**第一个检查点**：一行能确认或排除你的假设的代码或命令，以及期望看到的值。
4. 根因有两种常见触发，各写一句话。其中有一种**不会报任何错**，指出是哪一种以及为什么它静默。
5. 修好之后，你期望这三个观测量各变成多少？给数字（比值给两位小数，字节量给 MB）。
6. 这个故障属于三本账里的哪一本？为什么它在 64M 上**没有**表现为 OOM？

### M03-D-04（12 min · A1 · D）

一次单卡 smoke（MiniMind 默认 64M，fp16 AMP，$b = 24$，$T = 1024$）在第 1 步就 OOM。traceback 的关键几行是：

```
  File ".../torch/nn/functional.py", line ...., in scaled_dot_product_attention
  ...
RuntimeError: CUDA out of memory. Tried to allocate 384.00 MiB
(GPU 0; 31.75 GiB total capacity; 31.2x GiB already allocated; ...)
```

同一份代码在 $b = 8$、$T = 512$ 下正常跑完。

1. `384.00 MiB` 这个数字本身能告诉你什么？把它反推成一个 shape 和 dtype，给算式。
2. 由此判断 SDPA 落到了哪个后端。这个判断的依据是什么——为什么另外两个后端不会产生这个分配？
3. 用本周的激活估计式，把 $b=24$、$T=1024$ 下两个后端的每卡激活各算出来，说明为什么一个 OOM 另一个不会。数字要写清单位。
4. 为什么这个后端会被选中？给出至少两种可能的触发路径，并说明哪一种与 MiniMind 的 `Attention.forward` 直接相关。
5. 给出两个修复动作并排序。指出其中哪一个是**修根因**、哪一个是**绕过**，以及在什么情况下"绕过"反而是正确选择。
6. 这条边界对本周的实验设计施加了一个硬约束，写出那个约束是什么。

### M03-D-05（12 min · A1 · D）

一次完全 on-policy 的 GRPO smoke（V100，fp16 AMP，clip $\varepsilon = 0.2$），在**第一次内层更新之前**就观察到：

```
ratio: mean = 1.004   std = 0.061   p99 = 1.21   max|ratio - 1| = 0.34
frac_clipped_at_step0 = 0.031
```

1. 理论上这一步的 `ratio` 应该等于多少？给出理由（说清"完全 on-policy 且第一次内层更新之前"这两个条件各自起什么作用）。
2. 观测到的偏离有两种**性质完全不同**的归因。分别写出来，并说明为什么把它们混在一起这个实验就白做了。
3. 设计**一次**能把这两种归因分开的重算。写出：重算什么、控制哪些变量不变、要比较哪个量、两种结果各对应哪个结论。
4. 每个结论各给出下一步的具体修复方向（不是"提高精度"这种笼统说法，要落到某一段代码或某一条路径上）。
5. 在做这次重算之前，还有一件事必须先确认，否则你可能在诊断一个根本不存在的问题。写出那件事是什么。
6. 这个诊断动作的代价有多大？为什么"代价极小"本身是把它放在诊断第一步的理由？

---

## Design

### M03-P-01（15 min · A1 · E）

你手上只有 8×V100 32 GB（sm70、torch 2.1.0、不能新建环境、不能升级），要训一个 **27B** 的 dense 模型。

1. 先算账：纯 fp32 训练态（param + grad + Adam 两个矩）总共多少字节？8 卡分摊后每卡多少？和 32 GiB 的关系是什么？把这三个数写出来。
2. 针对**参数 / 梯度 / 优化器状态 / 激活**四项，各列出至少一个在这台机器上**真的可用**的压缩手段，每个给出压缩后的字节数或比例。
3. 把这些手段按"每 GB 收益 / 引入的风险"排序，写出你的顺序和排序依据。指出其中哪一个改变的是"你在训什么"而不只是"怎么训"。
4. 每个手段写一条**失败边界**：它在什么条件下不成立、或会引入什么静默错误。至少要覆盖到 8-bit 优化器状态那一条本机 CPU 实测得到的反直觉结论。
5. checkpoint 路径怎么选？把不可行的那个选项的具体数字写出来（为什么它在 27B 上直接不成立）。
6. 写出你在**碰 GPU 之前**就能做完的第一个可验证下一步：具体是哪个脚本、什么参数、你预期看到什么、看到什么就说明账算错了。

### M03-P-02（15 min · A1 · E）

设计一个能**证伪**"把冻结的 ref 模型量化到 INT8 不会改变 RL 的优化方向"这个论断的实验。

1. 写出 hypothesis（要写成可证伪的形式）、controlled variables、metric、stop condition、decision rule 五项。
2. metric 里必须有一个能区分"这个近似引入的是**噪声**还是**偏置**"的量。写出那个量怎么算，并解释为什么噪声和偏置在数值大小相同时后果完全不同。
3. 给出至少一个能直接支持"优化方向没变"的量，并写出你的通过阈值与阈值的来源。
4. 写出一个**退化情形**：在什么条件下你的 metric 会算出 `nan` 或 `inf`，这时候第一件该做的事是什么（不是去查量化代码）。
5. 写出这个实验的**失败边界**：即使它 PASS 了，你**不能**由此推出什么结论。至少给两条。
6. 写出实验结束后能带出公司的证据字段（只能是比值、布尔、计数、结果类型）。

### M03-P-03（15 min · A0 · E）

设计一次 fixed-global-batch 等价实验：证明 $W = 2$ 与 $W = 8$ 在全局 batch 相同时走出同一条曲线。

1. 写出被固定的那个量的完整表达式，以及两组配置各自的 $(W, A, b)$。
2. 写出**完整的控制变量清单**。至少要覆盖：随机性、数据顺序、初始化、精度与后端、学习率时钟、裁剪、以及 scaler 相关的一项。每一项写清"怎么控"。
3. 写出 metric（比较哪个序列上的哪个量）与判定阈值。**阈值必须给推导**：说清楚你允许的差异来自哪些具体的数值来源，为什么不能直接照搬同一 world_size 下重放实验用的那个更严的阈值。
4. 写出 stop condition 与 decision rule。
5. 实验不通过时，写出你的二分顺序（三步以内），每一步隔离掉哪一类原因。
6. 写出这个实验的**失败边界**：它 PASS 之后，仍然**不能**证明的两件事是什么。

### M03-P-04（15 min · A1 · E）

本周所有通信账的**字节数与次数都是可以算死的**，唯一不确定的是两个机器标量：$\alpha$（一次集合通信的固定启动延迟）与 $\beta$（有效带宽）。设计一次 Day 0 的 wiring smoke，用尽可能少的运行把这两个标量圈出来。

1. 写出你要测的量、扫描的自变量，以及从测量结果里**分离** $\alpha$ 与 $\beta$ 的方法（说清楚哪个是截距、哪个是斜率）。
2. 这次测量要在几个不同的 world_size 下做？为什么？
3. 圈出 $\alpha$、$\beta$ 之后，本周有一个具体的不等式会立刻变成可判定的。写出那个不等式，并说明判定为真 / 为假各会让你对后面哪一天的实验做出什么不同的预期。
4. 写出这次 smoke 的 stop condition 与 decision rule。
5. 写出**两条失败边界**：一条关于机器占用状态如何让某一类结论失效（以及失效之后还剩哪些量可用），一条关于单节点内混合拓扑如何让"一个 $\beta$"这个假设本身不成立。
6. 写出可以带出公司的证据字段。注意 $\alpha$ 与 $\beta$ 本身都是绝对量——说明你打算用什么替代量来报告结论。

---

## Trade-off

### M03-T-01（10 min · A1 · E）

fp16 AMP + `GradScaler` vs 纯 fp32，在 V100 上的三方权衡：

1. **字节账**：AMP 相对 fp32 到底省了什么、没省什么？把静态项与激活项分开说，各给一个数字或比例。特别说明 loss 头那一段的结论，以及为什么它反直觉。
2. **误差账**：AMP 引入了哪些新的失效模式？至少两条，每条给一个可判定的阈值或判据。
3. **时间账**：收益的来源是什么？在 V100 上的理论上界是多少倍？为什么实际达不到？
4. 什么情况下你会**选择纯 fp32**？给至少两个具体场景，每个说明是三本账里的哪一本在起决定作用。
5. 如果换到一台有 BF16 的卡上，上面四问的答案有哪几处会变？逐条说。

### M03-T-02（10 min · A1 · E）

权重量化的粒度选择：per-tensor / per-channel / per-group(g=128)。

1. 沿三个维度把它们排序：量化误差、scale 的存储开销、kernel 实现复杂度。每个维度说清排序方向。
2. per-channel 的 scale 存储开销怎么算？给一个表达式；说明这个开销占比随权重矩阵尺寸往哪个方向走，以及这对"迁移到超大模型"意味着什么。
3. per-tensor 换成 per-channel 通常能带来一个数量级的误差下降，其**机理**是什么？如果实测发现"per-channel 没有收益"，第一个检查点是什么？为什么这个错误特别危险。
4. per-group 相对 per-channel 的收益递减点由什么决定？在什么条件下你会判断"per-channel 就够了"。
5. 上面所有结论里，有哪些在 V100 上**能真实测量**、哪些**只能模拟**？分开列。

### M03-T-03（10 min · A1 · E）

QAT vs PTQ：

1. 各写一句话定义，并说明两者在 V100 上分别是"真跑"还是"模拟"。
2. 什么情况下 PTQ 就够了？给至少三个判断条件，每个尽量给可测量的形式。
3. QAT 的代价是什么？除了训练时间，还有一项容易被忽略的**数值代价**——它和 fp16 AMP 直接相关。写出那一项，并给出一个定量论证：说明这项误差能吃掉多大比例的量化预算。
4. 本机 CPU 实测得到了一条关于**截断阈值调优**的反直觉结论。写出那条结论（含三个位宽各自的最优阈值方向），并说明它对"要不要为 8-bit 权重量化去调截断阈值"这个决策意味着什么。
5. 如果自己写的 fake-quant 与框架实现对不上，有两个已知的具体原因。写出它们，并说明这对"该用什么判据做对照"的影响。

### M03-T-04（12 min · A1 · E）—— **Gate 题（A1 45 分钟中的后半）**

回答这个问题，并把它讲成一条完整的因果链：

> **为什么在 V100 上做 INT8 权重量化"省显存但不省时间"，而同一份代码在 A100/H100 上会省时间？**

要求覆盖：

1. V100 的 Tensor Core 接受什么输入？INT8 的 GEMM 在这台机器上只能走什么路径？把那条路径写成一个三段式的数据流。
2. 把收益拆成**字节账 / 带宽账 / 时间账**三行，每行写清是正、是零、还是负，并说明为什么其中一行可能是**负**的。
3. A100/H100 上变的是哪一行？变的机制是什么（要落到具体的硬件能力代际上，说出那个能力从哪一代开始有）。
4. 由此给出一个可迁移的判断规则：拿到一块新卡时，你怎么**在跑之前**判断某个量化方案能不能带来时间收益？
5. 如果在 V100 上实测发现 int8 路径**反而更快**，这不是好消息而是一个必须查清的信号。写出你会去查什么，以及为什么这个机制必须和"INT8 Tensor Core 加速"严格区分开。
6. 本周把量化还原成"一个误差预算问题 + 一个内存带宽问题"，这个选择的**代价**是什么？也就是说，有哪一类结论是这条路线永远给不出来的，要回答它需要什么条件。
